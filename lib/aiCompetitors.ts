// lib/aiCompetitors.ts
export const generateAICompetitorInsights = async ({
  title,
}: {
  title: string;
}) => {
  // Replace with real AI call
  return [
    `Similar platform to "${title}" in early-stage market`,
    `Competitor X is exploring a related niche`,
  ];
};
